PRAGMA journal_mode=OFF;PRAGMA synchronous=OFF;CREATE TABLE manifest_meta(key TEXT PRIMARY KEY,value TEXT NOT NULL);CREATE TABLE publish_units(id TEXT PRIMARY KEY,epoch_id TEXT NOT NULL,source_id TEXT NOT NULL,unit_key TEXT NOT NULL,required_count INTEGER NOT NULL,metadata_path TEXT NOT NULL DEFAULT '');CREATE TABLE entries(entry_id TEXT PRIMARY KEY,pack_id TEXT NOT NULL,pack_offset INTEGER NOT NULL,record_length INTEGER NOT NULL,source_id TEXT NOT NULL,epoch_id TEXT NOT NULL,logical_path TEXT NOT NULL,size INTEGER NOT NULL,sha256 TEXT NOT NULL,operation TEXT NOT NULL,publish_unit_id TEXT NOT NULL,package_key TEXT NOT NULL DEFAULT '',version TEXT NOT NULL DEFAULT '',metadata INTEGER NOT NULL,attributes BLOB);CREATE INDEX ix_entries_pack ON entries(pack_id,pack_offset);
